# Stage 1 — Learn the mechanics

Goal: understand base cases and shrinking input.

1. **Print numbers from `1` to `N`**
2. **Print numbers from `N` to `1`**
3. **Sum of first `N` natural numbers**
4. **Factorial of `N`**
5. **Calculate `a^b`**
6. **Count digits in an integer**
7. **Sum of digits of an integer**
8. **Reverse a string**
9. **Check if a string is a palindrome**
10. **Find maximum element in an array**

---

# Stage 2 — Process one element and recurse

Goal: think in terms of "solve smaller problem first".

11. **Find minimum element in an array**
12. **Sum of array elements**
13. **Check if an array is sorted**
14. **Count occurrences of a number in an array**
15. **Find first occurrence of an element**
16. **Find last occurrence of an element**
17.  **Remove a character from a string**
18. **Replace all occurrences of a character**
19. **Move all occurrences of a character to the end**
20. Remove consecutive duplicate characters 

Example:

```
aaabbccdaa -> abcda
```

# Stage 3 — Build recursive thinking

Goal: recursion that returns values and combines results.

21. Fibonacci number
22. Binary search recursively
23. Reverse an array recursively
24. Compute GCD using Euclid's algorithm
25. Convert decimal to binary recursively
26. Count ways to climb `n` stairs taking 1 or 2 steps
27. Tiling problem with `2 x n` board
28. Josephus problem
29. Tower of Hanoi
30. Fast exponentiation (`O(log n)`)

---

# Stage 4 — Choice recursion

Goal: understand branching recursion.

31. Print all subsequences of a string
32. Count subsequences
33. Print all subsets of an array
34. Generate all binary strings of length `n`
35. Generate all strings of length `n` using `{a,b,c}`
36. Count subsets with sum `k`
37. Generate all valid parentheses
38. Letter combinations of a phone number
39. Combination Sum
40. Subset Sum

---

# Stage 5 — Backtracking

Goal: become comfortable with undoing choices.

41. Permutations of a string
42. Permutations of an array
43. N-Queens
44. Rat in a maze
45. Sudoku solver
46. Word search
47. M Coloring Problem
48. Generate all palindromic partitions
49. Restore IP addresses
50. Combination Sum II