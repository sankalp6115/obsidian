Many recursive tree problems follow this pattern:

```
"What property should hold for these two roots?"
```

Examples:

- Same Tree → left == left AND right == right
- Symmetric Tree → left == right AND right == left
- Flip Equivalent Tree → (left == left AND right == right)  
    OR  
    (left == right AND right == left)